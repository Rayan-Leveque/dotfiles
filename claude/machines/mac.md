# Machine : Mac

Tu es sur le Mac personnel de l'utilisateur. Deux VPS sont accessibles en SSH (alias dans `~/.ssh/config`) : `contabo` (Claude Code nomade, port 2222, Tailscale 100.116.111.35) et `vps` (ancien — TeXLive, Maven, .NET).

Doc complète des VPS : `~/dotfiles/claude/projects/Contabo/CLAUDE.md` (symlinké dans `~/Documents/VPS/Contabo`).
